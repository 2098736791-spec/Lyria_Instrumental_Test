# 复制本文件为 .env 并填入真实值;生产路径(纯 B/透传)不需要任何 key
# 本服务专用 key(推荐,防共享服务器变量名冲突,见 README FAQ Q2)
INSTRUMENTAL_PROMPT_GEMINI_API_KEY=

# 机器上没有其他 Gemini 服务时,也可用通用变量(优先级低于上面)
GEMINI_API_KEY=

# 可选:设置后 /prompt 需要 Authorization: Bearer <token>;留空不鉴权
API_TOKEN=
