# 纯音乐约束实验 · 交付报告

> 2026-08-19。服务功能：轻度用户音乐生成的「纯音乐按钮」。
> 实验归档：`experiments/2026-08-no-vocals/`；本报告与 demo 同目录。

## 一、问题

Lyria API 无纯音乐开关（`instrumental`/`lyrics`/`duration` 字段不读取）。「纯音乐按钮」只能靠 prompt 文本层约束——把"纯音乐"写进 prompt，赌 Lyria 内部的语言模型转写层服从。

本轮实验验证三种写法的服从率，为后端选定实现方案。

## 二、实验设计

- **数据集**：37 条模拟用户输入，5 组覆盖（强制人声/合唱/呢喃等困难组 17 条、简单纯音组 4 条、英文组 6 条、专业结构化标签流组 10 条）
- **判定**：`generation.txt` 除 `[[A0]]` 段标外出现任何文本 = 含人声 = 失败（词在即人声在，无需人耳）
- **规模**：每臂 37×2 = 74 次生成，串行
- **模型**：lyria-3-pro-preview

## 三、结果

| 方法 | 形式 | 服从率 | LLM 成本 | 风险面 |
|---|---|---|---|---|
| A 裸前缀（4 变体中最优为 A4 中文句） | `这是一首纯音乐，不要歌词，不要人声。{输入}` | **84%**（46/55） | 零 | — |
| **B1 系统提示词包裹** | 规则框 + `<user_input>` 标签 | **100%**（35/35） | **零** | Lyria 转写层行为无 API 承诺 |
| **C1 LLM 预改写** | Gemini-3.5-flash 删人声改写为标签流 | **100%**（41/41） | 每次生成 +1 次 LLM 调用 | 改写质量/语义偏移/上游可用性 |

详细用例级对照见 `test_table.md`（同一用例在三种方法下的实测结果 + C1 改写结果 + 音频产物指针）。

## 四、第一种方式（裸前缀）的问题

**矛盾输入时无法剔除人声意图。** 约束句与用户的人声请求平级共存于文本中，转写层自行取舍：

- 演唱类请求（女声情歌/粤语老情歌/歌剧）：大多被压住（84% 的由来）
- **叙事型人声请求（念白、讲故事、呢喃）：几乎全漏**——2-2 女声念白 ×2 全败、2-5 睡前故事 ×2 全败
- 极端案例 1-5（"像小猫踩键盘"+"欢快合唱"）：旧数据集实测约束句被无视，模型把"喵喵喵"唱了出来

B1（结构化规则被转写层识别为指令）和 C1（人声词源头删除、根本不进 Lyria）都能解决这类矛盾输入，实测这些顽固用例全部通过。

## 五、后端建议

1. **两种可行实现，平行候选非主次**：
   - **B1**：零成本零延迟，规则框模板见 `demo.py` 的 `b1_wrap()`。风险在下游——Lyria 版本更新可能改变转写层服从性（复合写法自述消失是先例），建议上线后保留人声自动监控（判定逻辑见 README 第四节）
   - **C1**：控制权在上游，改写系统提示词与实现见 `rewrite_llm.py`。风险在 LLM 环节（改写波动、语义删减如 2-2 删至「纯音乐, 情歌」、上游服务可用性）
   - 组合策略（如 C1 失败回落 B1）属工程决策，本轮未验证
2. **注意版权拦截是静默失败**（`parts=None` 无报错），上游需过滤艺人名
3. **纯音乐时间戳不可得**：B1T/B1T2 探针（15 样本，含"指令本身即全部输入"的极限测试）证明 prompt 层无法让 Lyria 返回段落时间戳——该元数据需求走音频分析侧

## 六、遗留

- B1 会话 39 个、C1 会话 33 个网络断连窟窿（同 label 重启即补，判定结论不受影响）
- C1 音频未做人耳语义偏移抽查（文本判定全过；"改写是否删过头"需抽查听感）
- B2（一句话引号式）、A 组其余 6 变体未跑（A4 已证裸前缀天花板，无继续价值）
